unzip ai-biz-platform-devops.zip
cd ai-biz-platform-devops

git init
git add .
git commit -m "Initial DevOps skeleton for AI platform"
git branch -M main
git remote add origin <your-git-repo-url>
git remote -v
git push -u origin main

git remote set-url origin https://github.com/shankutanna/ai-biz-platform.git

git remote remove origin
git remote add origin https://github.com/shankutanna/ai-biz-platform.git
git push -u origin main

git lfs install  -------install git lfs

git lfs track "*.zip"  ......track large files
git lfs track "*.jar"
git lfs track "*.pt"

git add .gitattributes .....commit and push
git add .
git commit -m "Track large files with Git LFS"
git push

checking repo size locally - run inside hacathon
du -sh .
du -sh * | sort -h  ...this will expose whats big

now lets create .gitignore file 
touch .gitignore and paste below files
# dependencies
node_modules/

# build output
dist/

# env files
.env
.env.*

# logs
*.log

# OS / editor
.DS_Store
.vscode/

If node_modules already exists → remove from Git index
git rm -r --cached node_modules

Initialize Git and push (clean way)
git init
git branch -M main
git add .
git commit -m "Initial frontend commit (Vite + Tailwind)"
git remote add origin https://github.com/shankutanna/ai-biz-platform.git
git push -u origin main
